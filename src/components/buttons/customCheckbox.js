import { Checkbox, CheckboxGroup } from "@heroui/react";

export default function CustomCheckbox({label}) {
    
    return (
        // <CheckboxGroup 
        //     label={<h5 className="text-[var(--primary-blue-100)]">{label}</h5>}
        //     value={selectedValues}
        //     onChange={(values) => {
        //         setSelectedValues(values);
        //         handleStar(values);
        //     }}
        // >
        //     {options.map((option) => (
        //         <Checkbox 
        //             key={option.key} 
        //             value={option.key}
        //         >
        //             {option.label}
        //         </Checkbox>
        //     ))}       
        // </CheckboxGroup>
        (<>
        </>)
    );
}